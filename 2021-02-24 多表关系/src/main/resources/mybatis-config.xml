<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <properties resource="druid.properties" />

    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>

    <!-- 别名 -->
    <typeAliases>
        <!-- 一旦设置了别名，它是不区分大小写的 -->
        <typeAlias type="com.sms.common.DruidDataSourceFactory" alias="druid"></typeAlias>
    </typeAliases>

    <environments default="development">

        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="druid">
                <property name="driverClass" value="${dev.driverClassName}"/>
                <property name="url" value="${dev.url}"/>
                <property name="username" value="${dev.username}"/>
                <property name="password" value="${dev.password}"/>
                <property name="initialSize" value="${dev.initialSize}"/>
                <property name="maxActive" value="${dev.maxActive}"/>
                <property name="maxWait" value="${dev.maxWait}"/>
            </dataSource>
        </environment>

    </environments>
    <mappers>
        <mapper resource="mappers/person.xml"></mapper>
        <mapper resource="mappers/idCard.xml"></mapper>
        <mapper resource="mappers/bankCard.xml"></mapper>
    </mappers>
</configuration>
